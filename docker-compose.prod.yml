name: delivery-traceability-prod

services:
  backend:
    container_name: delivery-backend-prod
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - ./backend/.env
    expose:
      - "8000"
    restart: unless-stopped

  frontend:
    container_name: delivery-frontend-prod
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api
    ports:
      - "8082:80"
    depends_on:
      - backend
    restart: unless-stopped